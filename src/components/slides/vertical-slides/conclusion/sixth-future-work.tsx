function SixthFutureWork(){
return (
  <div className="full-screen-content" style={{display: "flex", justifyContent: "center", alignItems: "center"}}>
      <div>

<p><small>
• Application could be made deployable by following the steps in the git repository for wider audience reach
<br/><br/>
&#8594; If the backend made available for public, api end points could be documented with a tool such as "swagger" to help users have a better experience and a data valiadtion library like "pydantic" could be used to validate the user sent data(data sent for query generation, etc.) 
</small></p>
</div>
</div>
)
}

export default SixthFutureWork
