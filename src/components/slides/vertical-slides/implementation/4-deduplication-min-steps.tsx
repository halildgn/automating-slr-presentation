import deduplication from '../../../../assets/deduplication.gif'

function DeduplicationCli(){
return (
  <div className="full-screen-content" style={{display: "flex", justifyContent: "center", alignItems: "center"}}>
      <div>
<img src={deduplication} className="full-screen-content"/>
<p><small>
To just remove duplicates in the command line interface app, one would have to go through these steps
</small></p>
</div>
</div>
)
}

export default DeduplicationCli  


