function DependenciesOfCli(){
return (
  <div className="full-screen-content" style={{display: "flex", justifyContent: "center", alignItems: "center"}}>
      <div>
<p>
Also, the existing application is a pure Python script which forces the
user to install and keep all the dependencies to run the application. There had been attempts to freeze the application(bundling everything into a single executable binary), however this wasn't really successful.      
Many malware creators used application bundlers to bundle their Python malware into a virus. Therefore Windows anti-virus softwares treat the
pre-compiled bootloader binaries of these bundlers as viruses. Bundling was successful both for Linux and MacOS, however, to keep things consistent the decision was made not to bundle them.  
</p>
</div>
</div>
)
}

export default DependenciesOfCli  




